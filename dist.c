#include <math.h>
#include <stdio.h>
#include <stdlib.h>
#include "point.h"

float randFloat () /*random num between 0 & 1 for x or y coord*/
 {  return 1.0 * rand()/rand_max; }
int main (int argc, char *argv[]) 
float d;
int i,j,cnt;N;
/* enter declaration for array a here*/

for(i=0; i<N; i++)
/*generate random points and stores in array a*/
for (i=0; i<N; i++)
for (j=i+1; j<N; j++)
/*compare distance between two points with d and updates cnt if neccessary*/
printf("%d edges shorter than %.2f \n",           ,             );
}

